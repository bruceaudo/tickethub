import SellDashboard from '@/app/components/SellDashboard';
import React from 'react';

type pageProps = {
  
};

const page:React.FC<pageProps> = () => {
  
  return <>
    <SellDashboard />
  </>
}
export default page;